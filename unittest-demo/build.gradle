plugins {
    id 'java'
    id 'application'
}

application {
    mainClass = 'Main'
}
repositories {
    mavenCentral() // Download package from hub
}

dependencies {
    testImplementation('org.junit.jupiter:junit-jupiter:5.8.1') // junit version
}

test {
    useJUnitPlatform()
}